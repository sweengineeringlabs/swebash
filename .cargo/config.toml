# Local registry: Windows-native path for native Windows testing
[registries.local]
index = "file:///C:/Users/elvis/.cargo/registry.local/index"

# No source replacement needed for native Windows
# [source.old-ntfs-local]
# [source.linux-local]

# Native Windows: use default target directory
# [build]
# target-dir = "/tmp/swebash-target"
